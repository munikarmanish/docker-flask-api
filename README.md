# Dockerized Flask API

## Prerequisites

Install `docker` and `docker-compose`.

## Quickstart

Start the services:

    # ./up.sh

Stop the services:

    # ./down.sh

Run test cases on the API

    # ./test.sh
